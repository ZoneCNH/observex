{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Observex Error",
  "type": "object",
  "required": ["kind", "op", "message", "retryable"],
  "properties": {
    "kind": {
      "enum": [
        "config",
        "validation",
        "connection",
        "unavailable",
        "timeout",
        "auth",
        "conflict",
        "rate_limit",
        "canceled",
        "not_found",
        "already_exists",
        "internal"
      ]
    },
    "op": {
      "type": "string"
    },
    "message": {
      "type": "string"
    },
    "retryable": {
      "type": "boolean"
    }
  }
}
