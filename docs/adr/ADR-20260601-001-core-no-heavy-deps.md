# ADR-20260601-001 核心层不绑定重型观测依赖

## 状态

Accepted

## 背景

`observex` 要作为基础库模板被多个模块生成和复用。如果核心包直接依赖 Prometheus、OpenTelemetry、Zap 或 Logrus，下游库会被迫继承具体后端选择，升级和替换成本都会变高。

## 决策

核心包只保留轻量接口、常量、字段和值对象。具体后端放在上层 adapter 或调用方仓库中实现。

## 后果

好处是核心包依赖面小、生成库更容易独立验证。代价是使用方需要显式注入实际 logger、metrics 和 tracer 实现。
