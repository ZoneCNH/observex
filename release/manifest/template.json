{
  "module": "github.com/ZoneCNH/observex",
  "version": "v0.1.0",
  "commit": "{{COMMIT}}",
  "tree_sha": "{{TREE_SHA}}",
  "source_digest": "{{SOURCE_DIGEST}}",
  "tracked_file_count": 0,
  "go_version": "{{GO_VERSION}}",
  "generated_at": "{{GENERATED_AT}}",
  "generated_by": "scripts/generate_manifest.sh",
  "tree_state": "{{TREE_STATE}}",
  "checks": {
    "fmt": "{{CHECK_STATUS}}",
    "vet": "{{CHECK_STATUS}}",
    "lint": "{{CHECK_STATUS}}",
    "unit_test": "{{CHECK_STATUS}}",
    "race_test": "{{CHECK_STATUS}}",
    "boundary": "{{CHECK_STATUS}}",
    "secret_scan": "{{CHECK_STATUS}}",
    "security": "{{CHECK_STATUS}}",
    "contract": "{{CHECK_STATUS}}",
    "integration": "{{CHECK_STATUS}}"
  },
  "contracts": [
    {
      "path": "contracts/config.schema.json",
      "sha256": "{{CONFIG_SCHEMA_SHA256}}"
    },
    {
      "path": "contracts/error.schema.json",
      "sha256": "{{ERROR_SCHEMA_SHA256}}"
    },
    {
      "path": "contracts/field.schema.json",
      "sha256": "{{FIELD_SCHEMA_SHA256}}"
    },
    {
      "path": "contracts/health.schema.json",
      "sha256": "{{HEALTH_SCHEMA_SHA256}}"
    },
    {
      "path": "contracts/logger.schema.json",
      "sha256": "{{LOGGER_SCHEMA_SHA256}}"
    },
    {
      "path": "contracts/metric_naming.md",
      "sha256": "{{METRIC_NAMING_SHA256}}"
    },
    {
      "path": "contracts/metrics.md",
      "sha256": "{{METRICS_SHA256}}"
    },
    {
      "path": "contracts/metrics.schema.json",
      "sha256": "{{METRICS_SCHEMA_SHA256}}"
    },
    {
      "path": "contracts/public_api.md",
      "sha256": "{{PUBLIC_API_SHA256}}"
    },
    {
      "path": "contracts/public_api.snapshot",
      "sha256": "{{PUBLIC_API_SIGNATURE_SHA256}}"
    },
    {
      "path": "contracts/tracer.schema.json",
      "sha256": "{{TRACER_SCHEMA_SHA256}}"
    }
  ],
  "dependencies": [
    {
      "path": "github.com/ZoneCNH/observex",
      "main": true
    },
    {
      "path": "github.com/ZoneCNH/foundationx",
      "version": "v0.1.0"
    }
  ],
  "tools": {
    "go": "{{GO_VERSION}}",
    "golangci-lint": "{{GOLANGCI_LINT_VERSION}}",
    "govulncheck": "{{GOVULNCHECK_VERSION}}"
  },
  "artifacts": [
    "release/manifest/v0.1.0.json",
    "release/downstream/adoption.json"
  ],
  "notes": {
    "breaking_changes": "none",
    "downstream_evidence": "{{DOWNSTREAM_EVIDENCE}}",
    "known_risks": []
  },
  "downstream_adoption": {
    "fixture_smoke": {
      "status": "{{CHECK_STATUS}}",
      "fixtures": [
        {
          "name": "configx",
          "module": "github.com/ZoneCNH/configx",
          "package": "configx",
          "evidence": "scripts/run_integration.sh"
        },
        {
          "name": "corekit",
          "module": "example.com/acme/corekit",
          "package": "corekit",
          "evidence": "scripts/run_integration.sh"
        }
      ],
      "commands": [
        {
          "command": "GOWORK=off make integration",
          "status": "{{CHECK_STATUS}}",
          "exit_code": 0,
          "evidence": "scripts/run_integration.sh"
        }
      ]
    },
    "real_adoption": {
      "status": "blocked",
      "consumers": [],
      "blockers": [
        {
          "scope": "external_real_downstream",
          "reason": "No maintained external downstream repository is committed in this source tree; release evidence is fixture-backed until replaced by real repository commit and CI evidence.",
          "evidence": "release/downstream/adoption.json"
        }
      ]
    }
  }
}
